import 'package:flutter/material.dart';

class MenCategoryProducts extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Men Products'),
    );
  }
}
