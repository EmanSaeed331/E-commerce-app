import 'package:flutter/material.dart';

class GadgetsCategoryProducts extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Text(
        'Gadgets Products',
            style: TextStyle(
              color: Colors.purple,
            ),
      )),
    );
  }
}
